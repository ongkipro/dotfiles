---
name: pi-src
description: "pi-src — source code base for CLI tooling, maintained off local worktree unless actively needed"
metadata:
  node_type: memory
  type: project
  originSessionId: 0f8a9d42-9de8-4ea9-88f7-d5e7de9b7c77
  modified: 2026-08-05
---

`pi-src` refers to the `earendil-works/pi` repository (CLI source code), not a skill module.

State as of 2026-08-05:

- It has no permanent checkout under `~/Projects`.
- Clone it again from GitHub and use the `main` branch when source access is needed.
- Keep its project context here under `dotfiles/config/ai/project-memory` so it does not become scattered or get mistaken for a reusable skill.

Record future toolchain, dependency, or contribution-state changes in this file and the project-memory index at `dotfiles/config/ai/project-memory/MEMORY.md`.
