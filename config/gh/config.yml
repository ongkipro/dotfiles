# gh CLI config — DI-SYMLINK dari dotfiles ke ~/.config/gh/config.yml.
# Token OAuth TIDAK di sini: ada di ~/.config/gh/hosts.yml (mode 600, sengaja TIDAK di-sync).
#
# ⚠️ Schema harus LENGKAP sesuai versi gh terpasang. Kalau ada key yang hilang, gh menulis
# ulang file ini — dan karena ini symlink, tulisannya mendarat langsung di repo dotfiles
# sebagai diff liar. Sesudah upgrade gh: bandingkan dengan `gh config list`, lalu samakan.
#
# ⚠️ git_protocol di sini KALAH dari git_protocol per-host di hosts.yml.
# Efektifnya https — GitHub diakses lewat token, bukan kunci SSH (tak ada kunci GitHub di ~/.ssh).

# The current version of the config schema
version: 1
# What protocol to use when performing git operations. Supported values: ssh, https
git_protocol: https
# What editor gh should run when creating issues, pull requests, etc. If blank, will refer to environment.
editor: hx
# When to interactively prompt. This is a global config that cannot be overridden by hostname. Supported values: enabled, disabled
prompt: enabled
# Preference for editor-based interactive prompting. This is a global config that cannot be overridden by hostname. Supported values: enabled, disabled
prefer_editor_prompt: disabled
# A pager program to send command output to, e.g. "less". If blank, will refer to environment. Set the value to "cat" to disable the pager.
pager:
# Aliases allow you to create nicknames for gh commands
aliases:
    co: pr checkout
    prc: pr create --draft --fill
    prv: pr view --web
    prs: pr status
    rv: repo view --web
    is: issue status
    il: issue list
    runs: run list --limit 10
    prcleanup: '!gh pr list --state merged --limit 100 --json headRefName -q ".[].headRefName" | xargs -r -n1 git branch -D 2>/dev/null; true'
# The path to a unix socket through which to send HTTP connections. If blank, HTTP traffic will be handled by net/http.DefaultTransport.
http_unix_socket:
# What web browser gh should use when opening URLs. If blank, will refer to environment.
browser:
# Whether to display labels using their RGB hex color codes in terminals that support truecolor. Supported values: enabled, disabled
color_labels: disabled
# Whether customizable, 4-bit accessible colors should be used. Supported values: enabled, disabled
accessible_colors: disabled
# Whether an accessible prompter should be used. Supported values: enabled, disabled
accessible_prompter: disabled
# Whether to use an animated spinner as a progress indicator. If disabled, a textual progress indicator is used instead. Supported values: enabled, disabled
spinner: enabled
