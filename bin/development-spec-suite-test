#!/bin/sh
set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
REPO=$(CDPATH= cd -- "$SCRIPT_DIR/.." && pwd)
[ "${1:-}" = "$REPO" ] && shift
exec python3 "$REPO/skills/local/development-spec-suite/scripts/test-suite.py" "$@"
